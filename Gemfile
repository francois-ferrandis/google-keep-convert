# frozen_string_literal: true

source "https://rubygems.org"
ruby "2.6.6"

# Because we live in the Rails dictatorship and I love it
gem "activesupport"

# Parsing command line opts
gem "getoptlong"

# Query Joplin API
gem "flexirest"

# Because I love to be told what to do
gem "rubocop", require: false

group :test do
  gem "cucumber", require: false
  gem "rspec"
  gem "webmock"
end
